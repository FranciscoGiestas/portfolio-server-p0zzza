const db = require('../loaders/sequelize.loader').db;
//const Post = db.posts;

exports.create = async(content) => {
    //criar
    /*
    // Create a Tutorial
    const post = {
        title: content.title,
        description: content.description,
        published: content.published ? content.published : false
    };

    // Save Tutorial in the database
    await Post.create(post)
    .then(() => {
        console.log("wwhiskas boencas");
    });*/
};